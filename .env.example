# OpenClaw Session Viewer Environment Variables
# Copy to .env and adjust values as needed

# Directory containing OpenClaw agent session files
OC_AGENTS_DIR=~/.openclaw/agents

# Server settings
OC_PORT=8888
OC_HOST=0.0.0.0
OC_DEBUG=false

# Number of messages per page in the session view
OC_PAGE_SIZE=100
